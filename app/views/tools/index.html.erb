<div class="tool-index-container">


<div class="page-contain">

  <div class="info-card">
    <% if @query %>
      <h1>🔧Tools matching <br> <p style="margin-left: 2.6ch;"><%= @query %></p></h1>
    <% else %>
      <h1>Tools</h1>
    <% end %>
      <%= link_to "new tool", new_tool_path %>
    <div class="details-card">
      Hello
    </div>
  </div>

  <div class="index-container">
    <% @tools.each do |tool| %>
      <div class="index-tool">
        <div class="img-contain">
          <% if tool.photo.key != nil %>
            <%= cl_image_tag tool.photo.key, height: 120, width: 120 %>
          <% else %>
            <%=image_tag("tool-svgrepo-com.svg") %>
          <% end %>
        </div>
        <div class="name-desc">
          <h2><%= link_to tool.title, tool_path(tool) %></h2>
          <p class="ellipsis3 p_card"><%= tool.description %></p>
        </div>
        <div class="date-rate">
          <span><%= tool.rating %>★</span>
          <%= tool.created_at.strftime('%m/%Y') %>
        </div>
      </div>
      <% end %>
  </div>

</div>
